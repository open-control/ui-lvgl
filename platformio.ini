; Open Control - LVGL UI Integration

[platformio]
default_envs = dev

; ============================================================================
; Common settings shared by all environments
; ============================================================================
[env]
platform = teensy
board = teensy41
framework = arduino
build_flags =
    -std=gnu++17
    -I src
    -D LV_CONF_INCLUDE_SIMPLE
    -D LV_LVGL_H_INCLUDE_SIMPLE

; ============================================================================
; Development: uses local repos via symlink (requires repos in ../)
; The name= prefix must match the "name" field in library.json
; Usage: pio run -e dev
; ============================================================================
[env:dev]
lib_deps =
    open-control=symlink://../framework
    lvgl/lvgl@^9.4.0

; ============================================================================
; Release: uses GitHub repos (standalone, no local deps required)
; Usage: pio run -e release
; ============================================================================
[env:release]
lib_deps =
    https://github.com/open-control/framework
    lvgl/lvgl@^9.4.0
